{
  "name": "verify-demo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "^1.0.2",
    "twilio": "^2.5.2"
  },
  "devDependencies": {
    "twilio-run": "^3.5.3"
  },
  "engines": {
    "node": "16"
  }
}
